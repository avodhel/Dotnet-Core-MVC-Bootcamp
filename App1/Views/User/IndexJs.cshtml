@using Microsoft.AspNetCore.Http
@model List<UserViewModel>
@{
    ViewData["Title"] = "IndexJs";
}

<h1>Kullanıcılar üzerinde javascript ve jquery kullanarak işlem yapma</h1>
<strong>Bu sayfayı ilk ziyaret edişiniz @Context.Session.GetString("PageTimeStamp")</strong>

<div class="container">
    @if (TempData["Message"] != null)
    {
        <div class="alert alert-info"><p>@TempData["Message"]</p></div>
    }
    <div class="row">
        <div class="col-md-1"><h3>Sıra No</h3></div>
        <div class="col-md-2"><h3>Adı</h3></div>
        <div class="col-md-2"><h3>Soyadı</h3></div>
        <div class="col-md-2"><h3>Eposta</h3></div>
        <div class="col-md-5"><input id="newUser" type="button" class="btn btn-primary" value="Yeni Kullanıcı" data-toggle="modal" data-target="#modalTarget" /></div>
    </div>
    @foreach (var user in Model)
    {
        <div id="record-@user.Id" class="row">
            <div class="col-md-1"><p>@user.Id </p></div>
            <div class="col-md-2"><p>@user.Name </p></div>
            <div class="col-md-2"><p>@user.Surname </p></div>
            <div class="col-md-2"><p>@user.Email</p></div>
            <div class="col-md-5">
                <input id="detail-@user.Id" type="button" class="btn btn-info" value="Detay" data-toggle="modal" data-target="#modalTarget" />
                <input id="delete-@user.Id" type="button" class="btn btn-danger" value="Sil" data-toggle="modal" data-target="#modalTarget" />
            </div>
        </div>
    }
</div>

<div id="modalTarget" class="modal" tabindex="-1" role="dialog">
    <div class="modal-dialog" role="document">
        <div class="modal-content">
            <div class="modal-header">
                <h5 id="modalTitle" class="modal-title">Kullanıcı Detay</h5>
                <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                    <span aria-hidden="true">&times;</span>
                </button>
            </div>
            <div id="modalBody" class="modal-body">

            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-secondary" data-dismiss="modal">Kapat</button>
            </div>
        </div>
    </div>
</div>

<script src="~/js/User/user.js"></script>
<script src="~/js/Utils/cookie.js"></script>